namespace Florence

// longitude and latitude
// remarks: we don't a names for the tuple fields because that order (lon,lat) is prevalent in the World,
// except Google Maps Website (lat,lon)
type Coordinates = float * float

